# Optional API origin override (no trailing slash).
# Required for local/dev hosts that are not blog.<domain>.
# Production on blog.<domain> derives api.<domain> automatically.
# VITE_API_BASE=https://api.example.com
